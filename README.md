
  # Uniteam

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

   Run `npm run build` to build new version gh pages.

  Run `npm run deploy` to deploy new version gh pages.